import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.splash.screen',
  appName: 'ionic-splash-screen',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
